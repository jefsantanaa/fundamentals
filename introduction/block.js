{ /* To declare a block you must use a brace "{}", a block is a way of grouping 
  one or more statements or declarations in a program.
  */
 
  const x = 10; // Declaration of a variable x.
  console.log(x); // Statament that prints x.
  if (x > 5) { // Statament that checks a condition.
    console.log("Passed by here!"); // Statament that prints a message.
  }

}